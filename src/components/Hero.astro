---
interface Props {
  title: string;
  subtitle?: string;
  lead?: string;
}

const { title, subtitle, lead } = Astro.props;
---
<div class="hero">
  <h1>{title}</h1>
  {subtitle && <p class="subtitle">{subtitle}</p>}
  {lead && <p class="lead">{lead}</p>}
  <slot />
</div>
